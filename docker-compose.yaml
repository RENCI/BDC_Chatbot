version: '3.8'

services:
  renci-chatbot:
    build:
      context: .
      dockerfile: Dockerfile
    image: renci-chatbot:latest
    ports:
      - "8501:8501"
    env_file:
      - docker-env
    command: streamlit run streamlit_app.py --server.port=8501 --server.enableCORS=false
