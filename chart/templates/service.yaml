apiVersion: v1
kind: Service
metadata:
  name: {{ include "bdc-chatbot.fullname" . }}
  labels:
    {{- include "bdc-chatbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bdc-chatbot.selectorLabels" . | nindent 4 }}
