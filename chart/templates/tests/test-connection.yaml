apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bdc-chatbot.fullname" . }}-test-connection"
  labels:
    {{- include "bdc-chatbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bdc-chatbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
